/**
 * Default type for positioning components
 */
export interface Position {
	x: number
	y: number
}

/**
 * Default type for saving scale for components
 */
export interface Size {
	width: number
	height: number
}